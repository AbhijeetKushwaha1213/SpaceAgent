# ─────────────────────────────────────────────────────────────────────────────
# SENTINEL backend — Pinned Python dependencies (Phase 14 Security Requirement)
# ─────────────────────────────────────────────────────────────────────────────

# API framework & ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Data validation
pydantic==2.10.4

# Environment / config
python-dotenv==1.0.1

# LLM — Gemini Flash
google-genai==1.1.0

# Local / OpenAI compatible API client
openai==1.58.1

# Embeddings & Vector RAG
sentence-transformers==3.3.1
chromadb==0.6.3
llama-index-core==0.12.8
# Phase 17: PDF parsing for RAG ingestion. llama-index-core 0.14.x only reads
# PDFs via the optional llama-index-readers-file package; without a PDF
# reader it silently ingests raw bytes. pypdf is used directly in rag.py.
pypdf>=3.17.4

# Scientific & Utilities
numpy==2.2.1
scipy==1.15.1
httpx==0.28.1
